# dsh-plugin-market bundle patch: mounts the Plugin Workshop over the web
# profile. Host half: /api/dsh-plugin-market route (catalog from this repo's
# data/plugins.json + live GitHub stats + install/update ops). Client half:
# the "插件工坊 / Plugin Workshop" settings section plus the restart toast.

- insert:
    - id: dsh-plugin-market
      name: '@cyber-moshen/dsh-plugin-market'
